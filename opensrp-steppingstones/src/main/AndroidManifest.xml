<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.ei.opensrp.steppingstones" >

    <application
        android:name=".application.SteppingStonesApplication"
        android:allowBackup="true"

        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity
            android:name=".LoginActivity"
            android:screenOrientation="landscape"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NativeHomeActivity"
            android:theme="@style/AppTheme"
            android:screenOrientation="landscape"/>

        <activity
            android:name=".children.NativeChildrenSmartRegisterActivity"
            android:theme="@style/AppThemeNoActionBarAndTitle"
            android:screenOrientation="landscape"/>

    </application>

</manifest>
